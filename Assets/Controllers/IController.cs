namespace Reoria.Controllers
{
    /// <summary>
    /// Defines common controller properties and functions.
    /// </summary>
    public interface IController
    {
        // Nothing needs to be done in this interface, we just need it defined for inheritance.
    }
}
